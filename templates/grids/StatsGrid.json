{
	"category": "grids",
	"name": "StatsGrid",
	"displayName": "Stats Grid",
	"description": "Grid of big stats or text",
	"inputs": {
		"text-header": "string",
		"text-secondary": "string",
		"grid": [
			{
				"id": "string",
				"name": "string",
				"value": "string"
			}
		]
	}
}
